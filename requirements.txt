streamlit>=1.31.0
dashscope>=1.14.0
fpdf2>=2.7.0
pandas>=2.0.0
# Optional: funasr if needed locally, but we prioritize API
# funasr 
# torch  <-- We avoid specifying torch here to prevent conflict with pre-installed version on ModelScope
